stages:
  build
  test

create_file:
  image: alpine
  stage: build
  script:
      echo "Building..."
      mkdir build
      touch build/somefiles.txt

  artifacts:
    paths:
        build/

test_File:
  image: alphine
  stage: test
  script:
      test -f build/somefiles.txt